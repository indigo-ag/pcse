{
  "name": "indigoag-pcse",
  "version": "5.5.6",
  "description": "Indigo variant of the PCSE ensemble quantification model.",
  "scripts": {
    "install:deps": "pip install -r requirements.txt",
    "lint": "set -x && black --check --diff pcse/",
    "lint:fix": "mypy indigoag/ && yarn indigo-scripts",
    "test": "pytest"
  },
  "dependencies": {
    "yarn": "^1.22.19"
  },
  "license": "ISC"
}
